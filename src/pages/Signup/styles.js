import styled from 'styled-components';
import { FormLogin, LoginContainer } from '../../components/LoginForm/styles';

export const SignupContainer = styled(LoginContainer)``;

export const SignupForm = styled(FormLogin)``;